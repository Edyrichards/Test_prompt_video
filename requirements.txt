transformers==4.37.2
diffusers==0.27.2
huggingface-hub==0.20.3
accelerate==0.25.0

# Use torch 2.3.0 as it's the latest stable and matches torchvision/torchaudio
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

pillow==10.2.0
numpy==1.24.3
moviepy==1.0.3
opencv-python==4.9.0.80
ffmpeg-python==0.2.0
audiocraft==0.0.2
tts==0.17.1 ; python_version < "3.12"
# gradio 4.x depends on typer>=0.9,<1.0 which conflicts with
# spacy==3.5.2 (pulled in by audiocraft) requiring typer<0.8.0.
# Downgrade gradio and explicitly pin a compatible typer version.
gradio==3.50.2
typer==0.7.0
